# ------------------------------
# clean-gpt-json: .env.example
# ------------------------------

# Absolute path to the clean-gpt-json repository (where the code lives)
REPO_ROOT=/absolute/path/to/clean-gpt-json

# Absolute path to the external output directory (all results go here)
OUTPUT_ROOT=/absolute/path/to/doc-lib

# Apify credentials and task ID
APIFY_TOKEN=your_apify_api_key_here
APIFY_TASK_ID=your_username~your_task_name

# Optional: Used by shell alias `recover_apify_run` followed by the run ID (e.g. `recover_apify_run RqqAH8oPIFO3rnUr1`)
# No other variables required; this will be read by ragformatter.py and Makefile logic
